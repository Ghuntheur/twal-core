import React from 'react';
import IconButton from '@twal/components/ui/IconButton';

const Ui = () => (
  <section>
    <h1>UI kit default</h1>
    <IconButton
      icon='cancel'
      className="btn-icon"
    />
    <IconButton
      icon='cog'
      className="btn-icon"
    />
  </section>
);

export default Ui;
